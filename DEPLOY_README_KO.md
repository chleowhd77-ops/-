# D.J SPORTS ANALYTICS v1.5 배포 안내

이번 버전은 현재 잘 작동하는 경기 수집·분석·확률 계산을 건드리지 않고,
메뉴 구분과 관리자 공지·팝업, 인증 게시판 관리를 보강한 버전입니다.

## 가장 중요한 메뉴 구분

- `오늘의 TOP3`: 모든 방문자에게 보여주는 대표 추천 3경기입니다.
- `프로토 LIVE`: 베트맨에 등록된 경기만 보여줍니다. 수집된 베트맨 순서를 다시 정렬하지 않고 그대로 표시합니다.
- `전체 경기`: 해외·사설 이용자를 위한 세계 경기 전용 메뉴입니다. 프로토 LIVE 경기와 섞지 않습니다.
- `승무패 14`: 베트맨 승무패 14경기 전용 메뉴입니다.
- `채점 노트`: 종료 경기의 실제 결과와 예측을 비교합니다.
- `인증 게시판`: 모든 방문자가 읽을 수 있고 후원회원·관리자가 작성할 수 있습니다.

현재 `전체 경기` 메뉴는 독립 화면까지 준비되어 있습니다. AWS 수집기가
`all_matches`, `global_matches`, `world_matches` 중 하나로 세계 경기 자료를 보내기
시작하면 그 자료만 표시됩니다. 자료가 아직 없을 때 프로토 경기를 대신 섞어 넣지 않습니다.

## 이번에 추가된 기능

- 관리자가 첫 화면 띠 공지 또는 팝업 공지를 선택해서 등록
- 공지를 보여줄 대상 선택: 전체 / 비회원 / 일반회원 / 후원회원
- 공지 시작일·종료일·연결 주소 설정
- 팝업은 이용자가 이번 접속 동안 닫을 수 있음
- 작성자와 관리자가 게시글 제목·내용 수정
- 관리자가 문제 게시글을 삭제하지 않고 숨긴 뒤 다시 공개
- 적중 인증 글을 올리기 전 아래 내용을 반드시 확인
  - 경기가 종료된 뒤 작성했는지
  - 개인정보와 계좌번호를 가렸는지
- 경기 전 유료·선행 픽, 불법 사이트 홍보, 허위 수익 인증 금지 안내
- 기존 회원·게시글·사진·공지 자료가 있는 `users.db`에 필요한 항목만 자동 추가
- 데이터베이스 연결을 사용 후 바로 닫아 장시간 운영 안정성 보강

기존 기능도 유지됩니다.

- 회원가입·로그인·로그아웃
- 일반회원·후원회원·관리자 등급
- 후원회원 전환 요청과 관리자 승인·거절
- 관리자 회원 등급·상태 관리
- JPG·PNG·WEBP 인증 사진 1장(최대 2MB)
- 모바일 채점 노트 화면 넘침 방지
- 선택 메뉴를 큰 색상 버튼 대신 빨간 글씨로 표시
- D.J SPORTS ANALYTICS 로고와 다종목 확장형 브랜드

## GitHub에 올릴 파일

압축을 풀면 아래 파일이 보입니다.

1. `app.py`
2. `member_system.py`
3. `assets` 폴더
4. `DEPLOY_README_KO.md`
5. `STREAMLIT_SECRETS_EXAMPLE.toml`

GitHub 저장소 첫 화면에서 `Add file → Upload files`를 누르고 위 항목을 올립니다.
같은 이름이 나오면 덮어쓴 뒤 아래쪽 `Commit changes`를 누르면 됩니다.

이번 버전은 화면과 회원·게시판 기능만 바꿉니다. AWS의 `collector.py`,
`api_engine.py`, `config.py`는 바꾸지 않았으므로 AWS 수집 로봇을 재시작할 필요가 없습니다.

### 절대 올리거나 지우지 말아야 할 파일

- `users.db`를 GitHub에 올리지 마세요.
- 운영 중인 `users.db`를 삭제하지 마세요.
- 압축파일에는 `users.db`가 들어 있지 않습니다.

같은 `users.db`가 유지되는 환경에서는 앱 시작 시 기존 회원·게시글·사진을 그대로 두고
새 공지 항목만 자동으로 추가합니다. 다만 Streamlit Community Cloud의 로컬 저장소는
재배포나 서버 교체 때 사라질 수 있으므로 실제 회원 모집 전에는 AWS 영구 저장소나
PostgreSQL로 옮겨야 합니다.

## 올린 뒤 확인할 순서

1. GitHub에서 파일 업로드가 끝날 때까지 기다립니다.
2. Streamlit 화면을 새로고침합니다.
3. 메뉴가 아래 순서로 보이는지 확인합니다.
   - 오늘의 TOP3
   - 프로토 LIVE
   - 전체 경기
   - 승무패 14
   - 채점 노트
   - 인증 게시판
4. `프로토 LIVE`에서 베트맨 경기만 베트맨 순서대로 보이는지 확인합니다.
5. `전체 경기`에 프로토 경기가 억지로 복사되어 나오지 않는지 확인합니다.
6. 관리자로 로그인하고 왼쪽 `관리자 도구`를 엽니다.
7. `공지·팝업 관리`에서 팝업 공지 하나를 등록합니다.
8. 공지 대상과 기간을 바꿔 대상 회원에게만 보이는지 확인합니다.
9. 인증 게시판에서 글을 수정하고 관리자가 숨김·재공개할 수 있는지 확인합니다.
10. 적중 인증 글에서 필수 확인을 누르지 않으면 등록되지 않는지 확인합니다.

## 관리자 설정이 이미 된 경우

현재 화면에서 관리자 로그인이 잘 된다면 관리자 설정을 다시 할 필요가 없습니다.

관리자를 처음 지정할 때만 Streamlit 화면 오른쪽 아래 `Manage app`을 누른 뒤
설정 화면의 비밀값 입력란에 `STREAMLIT_SECRETS_EXAMPLE.toml`의 두 항목을 등록합니다.
관리자 비밀 인증키는 GitHub, 캡처 화면, 게시판에 절대 공개하지 마세요.

## 오늘의 TOP3가 바뀌는 시점

오늘의 TOP3는 화면에 고정한 목록이 아닙니다. AWS 수집 로봇이 새 경기와 분석 결과를
정상적으로 모아 `dashboard_data.json`을 갱신하면 다음 Streamlit 새로고침 때 다시 선정됩니다.

API 일일 한도를 모두 사용한 동안에는 마지막 정상 자료를 보존하므로 픽이 그대로일 수
있습니다. API 한도가 복구되고 AWS 수집이 한 번 정상 완료되어야 새 픽으로 바뀝니다.

## 상용화 전 반드시 할 일

- 회원 DB를 AWS 영구 저장소 또는 PostgreSQL로 이전
- 게시판 사진을 AWS S3 같은 영구 파일 저장소로 이전
- 정식 이용약관·개인정보 처리방침·운영자 문의 창구 확정
- 후원·광고·유료 서비스 관련 법률과 플랫폼 정책 확인
- 과장 수익·적중 보장 표현 금지 및 만 19세 안내 유지

이 배포본은 기존 분석 결과의 정확도를 임의로 높이거나 낮추지 않습니다. 메뉴와 회원·공지·
게시판 기능을 안전하게 정리한 버전이며, 분석 엔진 고도화와 세계 경기 수집 연결은 다음 단계입니다.
